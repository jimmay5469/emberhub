<h3 class='user-title'>
  <img class='avatar' src='{{model.user.avatarUrl}}' />
  <span>{{model.user.login}}</span>
</h3>
<ul>
  {{#each model.repositories as |repository|}}
    <li>{{repository.full_name}}</li>
  {{/each}}
</ul>